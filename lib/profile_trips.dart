import 'package:flutter/material.dart';

class ProfileTrip extends StatelessWidget {
  const ProfileTrip({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.amber,
    );
  }
}
